<h1>All Tags, eh?</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'article_title',           id: "tag_#{tag.id}"%>
    </li>
  <% end %>
</ul>

<%= link_to "<< All articles", articles_path %>